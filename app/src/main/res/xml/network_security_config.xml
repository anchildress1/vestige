<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security policy per ADR-001 §Q7. Cleartext is blocked everywhere; HTTPS is
    permitted only to the model-download hosts pinned in :core-model's
    /model/manifest.properties → allowed_hosts. Phase 1's download probe pinned the live
    redirect chain as huggingface.co -> cas-bridge.xethub.hf.co on 2026-05-09; keep the two
    allowlists in lockstep if the host chain changes again.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">huggingface.co</domain>
        <domain includeSubdomains="true">cas-bridge.xethub.hf.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
