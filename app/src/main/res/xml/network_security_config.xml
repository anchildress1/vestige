<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security policy per ADR-001 §Q7. Cleartext is blocked everywhere; HTTPS is
    permitted only to the model-download hosts pinned in :core-model's
    /model/manifest.properties → allowed_hosts. The actual redirect chain is captured during
    the Phase-1 download probe (Story 1.9) and may add LFS/Xet hosts to this allowlist —
    update both files in lockstep when that happens.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">huggingface.co</domain>
        <domain includeSubdomains="true">cdn-lfs.huggingface.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
