package dev.anchildress1.vestige.ui.onboarding

import android.app.Application

/** Stub Application — bypasses `VestigeApplication` so tests stay BoxStore-free. */
internal class OnboardingTestApplication : Application()
