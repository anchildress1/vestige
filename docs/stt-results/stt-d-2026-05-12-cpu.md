# STT-D run — 2026-05-12 (CPU)

## Setup

| Field | Value |
|---|---|
| Date | 2026-05-12 |
| Device | Galaxy S24 Ultra (SM-S928U, Android 16) |
| Model | `gemma-4-E4B-it.litertlm` |
| Backend | CPU |
| Manifest | `docs/stt-d-manifest.example.txt` (15 A-D scenario entries) |
| Threshold | `ceil(15 × 30%) = 5` entries with meaningful divergence |
| Harness commit | `ea6e4bb` (phase-2/stt-d-evidence) |
| Wall clock | 36m 43s |

## Verdict

**PASS — 12/15 (80%) meaningful divergence.** Threshold cleared by 7 entries.

All 15 entries × 3 lenses parsed cleanly on the first attempt; no retries, no
parse failures, no embedder errors.

## Per-entry summary

| ID | Lenses parsed | Latency (s) | Meaningful | Disagree fields | Skeptical flags | Notes |
|---|---|---|---|---|---|---|
| A1 | 3/3 | 144.2 | ✅ | `tags` | 1 (`state-behavior-mismatch`) | Strongest signal — both tag drift AND Skeptical contradiction flag |
| A2 | 3/3 | 144.2 | ❌ | — | 0 | All three lenses returned identical `[meeting, late-night]` |
| A3 | 3/3 | 156.9 | ✅ | `tags` | 0 | INFERENTIAL drops `roadmap-call`; LIT/SKEP keep it |
| A4 | 3/3 | 134.1 | ✅ | `tags` | 0 | SKEP renames `battery-yanked` → `battery-drain`, adds `sync` |
| A5 | 3/3 | 137.5 | ✅ | `state_shift` | 0 | SKEP flips `state_shift=true` (LIT/INF=false); all agree on tags + energy |
| A6 | 3/3 | 139.4 | ✅ | `tags` | 0 | LIT keeps `not`; INF/SKEP merge to `not-awake` |
| B1 | 3/3 | 138.7 | ✅ | `tags` | 0 | LIT uses `closing-it`; INF/SKEP normalize to `closing-task` |
| B2 | 3/3 | 156.9 | ✅ | — | 1 (`commitment-without-anchor`) | All three lenses agree on fields; SKEP catches the commitment slip |
| B3 | 3/3 | 137.3 | ❌ | — | 0 | Convergence on `[invoice, cursor]` — entry is unambiguous |
| C1 | 3/3 | 142.7 | ✅ | `tags` | 0 | LIT keeps `task-app`; INF/SKEP expand to `task-app-selection` |
| C2 | 3/3 | 142.6 | ❌ | — | 0 | All three return `[task-app, decision-loop]` (order ignored by canonicalize) |
| C3 | 3/3 | 148.5 | ✅ | `tags` | 0 | INF adds `exposure-therapy`; LIT/SKEP omit |
| D1 | 3/3 | 154.9 | ✅ | `tags` | 0 | LIT keeps `administrative-haunting`; INF/SKEP swap for `rearranging` |
| D2 | 3/3 | 151.9 | ✅ | `tags` | 0 | LIT keeps `folder-structure`; INF/SKEP elevate to `decision-loop` |
| D3 | 3/3 | 150.3 | ✅ | `tags` | 0 | SKEP keeps timestamp `4:07am` literal; LIT/INF normalize to `4am` |

**Latency:** min 134.1s, max 156.9s, mean 144.5s. No retries.

## Raw per-lens evidence

Below: every (entry × lens) cell with full `fields` map + Skeptical flag list. Raw lines are
copied verbatim from `stt-d-2026-05-12-cpu.raw.log`.

### A1 — "Standup ran long again..."

```
LITERAL     fields={tags=[standup, doc], energy_descriptor=flattened, state_shift=true,
                    vocabulary_contradictions=[], stated_commitment=null,
                    recurrence_link=null, recurrence_kind=null}
            flags=[]
INFERENTIAL fields={tags=[standup, late-night, doc, staring], energy_descriptor=flattened,
                    state_shift=true, vocabulary_contradictions=[], stated_commitment=null,
                    recurrence_link=null, recurrence_kind=null}
            flags=[]
SKEPTICAL   fields={tags=[standup, doc, flattened], energy_descriptor=flattened,
                    state_shift=true, vocabulary_contradictions=[], stated_commitment=null,
                    recurrence_link=null, recurrence_kind=null}
            flags=[state-behavior-mismatch: "I was fine before it, then completely flattened by 11."
                   — The user states they were fine but then describes a state change.]
```

### A2 — "Tuesday meeting. Same thing..."

```
LITERAL     fields={tags=[meeting, late-night], state_shift=false, [rest null]} flags=[]
INFERENTIAL fields={tags=[meeting, late-night], state_shift=false, [rest null]} flags=[]
SKEPTICAL   fields={tags=[meeting, late-night], state_shift=false, [rest null]} flags=[]
```

Convergence — no divergence signal.

### A3 — "After the roadmap call I did the little post-meeting corpse routine..."

```
LITERAL     fields={tags=[roadmap-call, post-meeting, corpse-routine, tabs-open, zero-movement], ...} flags=[]
INFERENTIAL fields={tags=[post-meeting, corpse-routine, tabs-open, zero-movement], ...} flags=[]
SKEPTICAL   fields={tags=[roadmap-call, post-meeting, corpse-routine, tabs-open, zero-movement], ...} flags=[]
```

INFERENTIAL drops the literal `roadmap-call` anchor.

### A4 — "Not tired exactly. More like the battery got yanked after the sync."

```
LITERAL     fields={tags=[battery-yanked], ...} flags=[]
INFERENTIAL fields={tags=[battery-yanked], ...} flags=[]
SKEPTICAL   fields={tags=[battery-drain, sync], ...} flags=[]
```

SKEPTICAL reframes "battery-yanked" as `battery-drain` and surfaces the `sync` anchor.

### A5 — "After the client review I went hollow..."

```
LITERAL     fields={tags=[client-review, hollow], energy_descriptor=hollow, state_shift=false, ...} flags=[]
INFERENTIAL fields={tags=[client-review, hollow], energy_descriptor=hollow, state_shift=false, ...} flags=[]
SKEPTICAL   fields={tags=[client-review, hollow], energy_descriptor=hollow, state_shift=true, ...} flags=[]
```

Only divergence on `state_shift` — SKEPTICAL flips it true; the lens reads "I went hollow" as a
state change despite no explicit "before/after" anchor.

### A6 — "The planning call left static in my head..."

```
LITERAL     fields={tags=[planning-call, static, awake, not], ...} flags=[]
INFERENTIAL fields={tags=[planning-call, static, awake, not-awake], ...} flags=[]
SKEPTICAL   fields={tags=[planning-call, static, awake, not-awake], ...} flags=[]
```

LITERAL keeps `not` as a bare token; the other two merge into the compound `not-awake`.

### B1 — "Invoice email is still sitting there..."

```
LITERAL     fields={tags=[invoice-email, closing-it], ...} flags=[]
INFERENTIAL fields={tags=[invoice-email, closing-task], ...} flags=[]
SKEPTICAL   fields={tags=[invoice-email, closing-task], ...} flags=[]
```

LITERAL keeps `closing-it` (verbatim from "closing it"); the others normalize to `closing-task`.

### B2 — "Said I would send the invoice today. Instead I reorganized the desktop..."

```
LITERAL     fields={tags=[invoice, desktop-reorganization], stated_commitment={text="I would send the invoice today",
                    topic_or_person=invoice}, ...} flags=[]
INFERENTIAL fields={tags=[invoice, desktop-reorganization], stated_commitment={text="I would send the invoice today",
                    topic_or_person=invoice}, ...} flags=[]
SKEPTICAL   fields={tags=[invoice, desktop-reorganization], stated_commitment={text="I would send the invoice today",
                    topic_or_person=invoice}, ...}
            flags=[commitment-without-anchor: "reorganized the desktop"
                   — The commitment to send the invoice lacks a concrete object for the reorganization.]
```

All three lenses agree on every comparable field; SKEPTICAL adds the slip flag that captures the
demo's hero pattern — the commitment vs the actual action.

### B3 — "Invoice again. No mystery. I know what to do..."

```
LITERAL/INFERENTIAL/SKEPTICAL: all return tags=[invoice, cursor], rest null. flags=[]
```

Convergence — no divergence signal.

### C1 — "Spent 40 minutes choosing between the two task apps..."

```
LITERAL     fields={tags=[task-app, decision-loop], ...} flags=[]
INFERENTIAL fields={tags=[task-app-selection, decision-loop], ...} flags=[]
SKEPTICAL   fields={tags=[task-app-selection, decision-loop], ...} flags=[]
```

LITERAL keeps the literal noun `task-app`; the other two reframe the activity as `task-app-selection`.

### C2 — "Task app decision still looping..."

```
LITERAL/INFERENTIAL/SKEPTICAL: all return tags=[task-app, decision-loop] (member order varies but
canonicalize() normalizes), rest null. flags=[]
```

Convergence — same identity as Story 2.7's only-unanimous entry. Architecture-stable behavior.

### C3 — "Made another comparison table..."

```
LITERAL     fields={tags=[comparison-table], ...} flags=[]
INFERENTIAL fields={tags=[comparison-table, exposure-therapy], ...} flags=[]
SKEPTICAL   fields={tags=[comparison-table], ...} flags=[]
```

INFERENTIAL alone reads the "exposure therapy" idiom as a tag-worthy anchor.

### D1 — "3:12am. I am rearranging the notes app again..."

```
LITERAL     fields={tags=[late-night, notes-app, administrative-haunting], ...} flags=[]
INFERENTIAL fields={tags=[late-night, notes-app, rearranging], ...} flags=[]
SKEPTICAL   fields={tags=[late-night, notes-app, rearranging], ...} flags=[]
```

LITERAL preserves the user's coined phrase `administrative-haunting`; the others replace it with
the deterministic `rearranging`.

### D2 — "2:48am and I'm deciding whether to rebuild the folder structure..."

```
LITERAL     fields={tags=[late-night, folder-structure], ...} flags=[]
INFERENTIAL fields={tags=[late-night, decision-loop], ...} flags=[]
SKEPTICAL   fields={tags=[late-night, decision-loop], ...} flags=[]
```

LITERAL captures the literal object (`folder-structure`); the others reframe the activity as a
`decision-loop` — a pattern-level abstraction.

### D3 — "4:07am. I found a naming convention problem in the archive..."

```
LITERAL     fields={tags=[4am, archive, naming-convention], ...} flags=[]
INFERENTIAL fields={tags=[4am, archive, naming-convention], ...} flags=[]
SKEPTICAL   fields={tags=[4:07am, archive, naming-convention], ...} flags=[]
```

SKEPTICAL keeps the exact timestamp; the other two round to `4am`. Minor tag drift but enough
for the divergence check.

## Observations

1. **80% divergence rate** on a 15-entry corpus is a stronger signal than the original 6-entry
   83% (5/6) from 2026-05-10 — sample size up by 2.5×, divergence rate barely moves.
2. **Convergence cluster:** A2, B3, C2 — all three are short, unambiguous entries where the
   model has little room to interpret. Architecture-stable; not a defect.
3. **`tags` is the dominant divergence axis** (11 of 12 meaningful entries). LITERAL preserves
   verbatim user phrases; INFERENTIAL + SKEPTICAL reframe to pattern-level abstractions. This is
   exactly the divergence the multi-lens design intends to surface.
4. **Skeptical flags fired on 2 entries** (A1 state-behavior-mismatch, B2 commitment-without-anchor).
   Both are the kind of slip the persona-injected observation prompt cites — strong demo signal.
5. **`state_shift` divergence on A5** is the only non-tags field disagreement. SKEPTICAL reads
   "went hollow" as a state change despite the prompt's no-anchor rule.
6. **No parse failures, no retries** — all 45 lens calls (15 × 3) succeeded on first attempt.

## Pipeline

Raw logcat at `stt-d-2026-05-12-cpu.raw.log` (138 lines). Generated by:

```bash
adb logcat -c
./gradlew :app:connectedDebugAndroidTest \
  -PmodelPath=/data/local/tmp/gemma-4-E4B-it.litertlm \
  -PmanifestPath=/data/local/tmp/stt-d-manifest.txt \
  -PinferenceBackend=cpu \
  -Pandroid.testInstrumentationRunnerArguments.class=dev.anchildress1.vestige.SttDLensDivergenceTest
adb logcat -d -s VestigeSttD > docs/stt-results/stt-d-2026-05-12-cpu.raw.log
```
