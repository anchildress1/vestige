pre-commit:
  parallel: false
  commands:
    ktlint-format:
      glob: '**/*.{kt,kts}'
      run: '"$(command -v ktlint || echo "$HOME/.local/bin/ktlint")" --log-level=error -F {staged_files}'
      stage_fixed: true
    detekt:
      glob: '**/*.{kt,kts}'
      run: make detekt
    secret-scan:
      run: make secret-scan
    actionlint:
      glob: '.github/workflows/*.yml'
      run: actionlint {staged_files}

commit-msg:
  commands:
    commitlint:
      run: make commitlint COMMIT_MSG_FILE={1}

pre-push:
  parallel: true
  commands:
    ktlint:
      priority: 1
      run: make ktlint-check
    build:
      priority: 2
      run: make build
    test:
      priority: 2
      run: make test-full
