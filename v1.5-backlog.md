# v1.5 Backlog

Features deferred from v1 because they fail the scope gate:

> Does it visibly improve the 90s pitch or 5-min walkthrough?

Anything that fails the gate but the user still wants → log here, do not implement.

## Format

Append entries chronologically. One bullet per item. Brief reason.

```
- [YYYY-MM-DD] <feature> — <why deferred>
```

## Entries

- [2026-05-08] detekt — deferred. 1.23.x calls `ReportingExtension.file(String)` (deprecated, removed in Gradle 10) inside its plugin apply, so the scaffold cannot enable it without breaking the warnings-as-errors gate. 2.0 is still alpha at scaffold time. Re-add when detekt 2.0 ships stable; ktlint covers the load-bearing rules until then.
